#include "BitmapEncoder.h"

//Gdiplus::Bitmap* Parga_PI::BitmapEncoder::pixToGdiplusBitmap(Pix* image, Gdiplus::PixelFormat pixelFormat)
//{
//
//	l_int32 w = pixGetWidth(image),
//		h = pixGetHeight(image),
//		d = pixGetDepth(image),
//		stride = d * w;
//
//	BYTE* data0 = (BYTE*)pixGetData(image);
//
//	return new Gdiplus::Bitmap(w, h, stride, pixelFormat, (BYTE*)data0);
//}
